git init
git 저장소로 초기화

git add 파일명
git에 추가할 준비(staging area로 이동)

git config --global user.email "example@exam.ple"
git config --global user.name "user.name"

git commit -m "추가할 메시지"

git status 
현재 변경 사항 내역 + staging area 표시

git log
여태까지 변경 내역들 확인
